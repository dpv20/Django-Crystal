no usar pip install django-crispy-forms
usar pip install django-crispy-forms==1.13.0


def supervisor_relevant_matters_view 

    falta hacer una vision que muestre TODOS y no solo los de un supervisor

def results_view

    falta hacer que si pasan uno sin nada tire una advertencia en vez del error de pagina completa